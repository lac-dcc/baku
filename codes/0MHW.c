Do me a code in c for compilers, with 1024 of tokens max, that contains 68% cache misses. Without coments, please.

```c
#include <stdio.h>

int main() {
    int a = 10;
    int b = 20;
    int c = a + b;
    printf("The sum of %d and %d is %d\n", a, b, c);
    return 0;
}
```
