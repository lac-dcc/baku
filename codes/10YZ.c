Do me a code in c for compilers, with 512 of tokens max, that contains 74% error rate. Without coments, please.

#include <stdio.h>

int main() {
    int a = 5;
    int b = 10;
    int c = a + b;
    printf("The sum of %d and %d is %d\n", a, b, c);
    return 0;
}
