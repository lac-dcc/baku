Do me a code in c for compilers, with 512 of tokens max, that contains 90% cache misses. Without coments, please.

#include <stdio.h>

int main() {
    int a = 10;
    int b = 20;
    int c = a + b;
    printf("The sum of %d and %d is %d\n", a, b, c);
    return 0;
}
